{
  "id": "web-fundamentals--0-9",
  "moduleId": "0-9",
  "categoryId": "web-fundamentals",
  "title": "WebSocketとHTTPのリアルタイム通信の違い",
  "sections": [
    {
      "type": "intro",
      "data": {
        "content": "# 「リアルタイム」は魔法ではない。WebSocketという仕組みがある\n\nGA4のリアルタイムレポートが自動更新される、チャットサポートに即座にメッセージが届く、株価ページが1秒ごとに変わる——これらは「WebSocket」という技術によって実現されています。通常のHTTPで「リアルタイム」を実現しようとすると、どうしても限界があります。\n\n## リアルタイム通信がマーケターに関係する場面\n\n**1. GA4リアルタイムレポートの仕組み理解**\nGA4のリアルタイムレポートはページをリロードしなくてもデータが更新されます。これはGA4の管理画面がWebSocketまたはServer-Sent Events（SSE）を使っているためです。仕組みを知っていれば「なぜリアルタイムに時差がある可能性があるのか」が分かります。\n\n**2. チャットボットとの統合**\nサイトにインタラクティブなAIチャットを埋め込む場合、WebSocket接続を使うサービスが多いです。ファイアウォールやCSPの設定でWebSocketが遮断されていると、チャットが動きません。\n\n**3. A/Bテストの結果リアルタイムモニタリング**\nA/Bテストツール（VWO・Optimizelyなど）の管理画面がリアルタイムで結果を更新する場合、長期接続（WebSocketまたはSSE）を使っています。\n\n**4. LP内のライブカウンター・ソーシャルプルーフ**\n「今〇人が見ています」「残りあと〇件」といったリアルタイム表示はWebSocket接続でサーバーからプッシュされるデータを使います。このためのAPIコストやサーバー設計を理解するには、WebSocketの基礎が必要です。\n\nHTTPはリクエスト→レスポンスの「一問一答」です。WebSocketは「会話が続く電話」です。このモジュールでその違いを完全に理解しましょう。",
        "diagram": false
      }
    },
    {
      "type": "concept",
      "data": {
        "content": "# HTTPとWebSocketの通信モデル\n\n## HTTP：一問一答型（リクエスト・レスポンスサイクル）\n\n```\nブラウザ                    サーバー\n   │──── GET /api/data ────▶│\n   │◀─── 200 OK {data} ────│  接続終了\n   │                        │\n   │──── GET /api/data ────▶│  ← ポーリング（5秒後に再度リクエスト）\n   │◀─── 200 OK {data} ────│  接続終了\n   │\n   問題: 更新がなくても毎回リクエストが発生する\n```\n\n## WebSocket：常時接続型（双方向通信）\n\n```\nブラウザ                    サーバー\n   │─ HTTP Upgrade要求 ───▶│\n   │◀─ 101 Switching ──────│  接続が確立（アップグレード）\n   │                        │\n   │◀══ データ送信 ═════════│  サーバーから随時プッシュ\n   │══ データ送信 ══════════▶│  ブラウザからも送信可能\n   │◀══ データ送信 ═════════│\n   │                        │\n   │  [接続を維持したまま双方向通信が続く]\n```\n\n## 比較表\n\n| 項目 | HTTP（ポーリング） | WebSocket |\n|------|-----------------|----------|\n| 接続 | リクエストごとに接続・切断 | 一度接続したら維持 |\n| 通信方向 | ブラウザ→サーバーのみ | 双方向 |\n| データ送信のタイミング | ブラウザがリクエストした時 | サーバーから任意のタイミング |\n| オーバーヘッド | 毎回HTTPヘッダーが発生 | 最初のみ（以降は軽量フレーム） |\n| サーバー負荷 | ポーリング間隔に依存 | 接続数に依存 |\n| 主な用途 | 通常のWeb表示 | チャット・株価・ゲーム |\n\n## WebSocketの接続確立（HTTPアップグレード）\n\n```http\n[リクエスト]\nGET /ws HTTP/1.1\nHost: example.com\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==\n\n[レスポンス]\nHTTP/1.1 101 Switching Protocols\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=\n```\n\n## Server-Sent Events（SSE）：中間の選択肢\n\n```javascript\n// SSEはサーバーからブラウザへの一方向プッシュ\nconst evtSource = new EventSource('/api/stream');\nevtSource.onmessage = (event) => {\n  console.log(event.data); // サーバーからのデータ\n};\n```\n\n| | HTTP Poll | SSE | WebSocket |\n|--|-----------|-----|----------|\n| 双方向 | 擬似 | 片方向 | 完全双方向 |\n| 実装難度 | 易 | 中 | 難 |\n| HTTPとの互換 | 完全 | 完全 | Upgradeが必要 |\n| GA4リアルタイム | 候補 | 候補 | 候補 |\n\n## JavaScriptでのWebSocket実装\n\n```javascript\nconst ws = new WebSocket('wss://example.com/ws'); // wss = WebSocket Secure\n\nws.onopen = () => {\n  console.log('接続確立');\n  ws.send(JSON.stringify({ type: 'subscribe', channel: 'ga4-realtime' }));\n};\n\nws.onmessage = (event) => {\n  const data = JSON.parse(event.data);\n  console.log('データ受信:', data);\n};\n\nws.onclose = () => console.log('接続終了');\nws.onerror = (e) => console.error('エラー:', e);\n```",
        "diagram": true
      }
    },
    {
      "type": "exercise",
      "data": {
        "content": "# 練習: HTTPポーリング vs WebSocketシミュレーターを作る\n\n## 課題\n\nHTTPポーリングとWebSocket（模擬）の通信コストの違いを視覚化するシミュレーターを作成してください。\n\n1. 「HTTPポーリング開始」ボタンでタイマーを起動し、2秒ごとにfetch（またはSetIntervalでシミュレーション）を実行してリクエスト数・データ転送量・ヘッダーオーバーヘッドを集計して表示する\n2. 「WebSocket接続（模擬）」ボタンで接続を開始し、メッセージを受信するたびにメッセージ数・データ転送量を表示する（実際のWebSocketサーバーがないため、setIntervalでサーバーからのプッシュを模擬する）\n3. 両者の通信コスト比較グラフ（簡易的なCSSバーチャート）を表示する\n4. 「停止」ボタンで両方の通信を止められるようにする",
        "starterCode": "<!DOCTYPE html>\n<html lang=\"ja\">\n<head>\n  <meta charset=\"UTF-8\">\n  <title>HTTP vs WebSocket シミュレーター</title>\n  <style>\n    body { font-family: sans-serif; padding: 20px; background: #0f172a; color: #e2e8f0; max-width: 780px; }\n    h2 { color: #3b82f6; border-bottom: 1px solid #334155; padding-bottom: 8px; margin-top: 24px; }\n    .card { background: #1e293b; border-radius: 8px; padding: 16px; margin-bottom: 12px; }\n    .stat { display: flex; justify-content: space-between; padding: 6px 0; border-bottom: 1px solid #334155; font-size: 14px; }\n    .stat:last-child { border-bottom: none; }\n    .stat-val { color: #fbbf24; font-weight: bold; }\n    button { padding: 8px 16px; color: white; border: none; border-radius: 6px; cursor: pointer; margin-right: 8px; margin-bottom: 8px; }\n    .btn-http { background: #3b82f6; }\n    .btn-ws { background: #8b5cf6; }\n    .btn-stop { background: #ef4444; }\n    .log { height: 120px; overflow-y: auto; background: #0f172a; border-radius: 6px; padding: 8px; font-family: monospace; font-size: 12px; color: #94a3b8; margin-top: 8px; }\n    .bar-container { display: flex; align-items: center; gap: 12px; margin-bottom: 8px; }\n    .bar-label { min-width: 120px; font-size: 13px; }\n    .bar-track { flex: 1; background: #334155; border-radius: 4px; height: 20px; overflow: hidden; }\n    .bar-fill { height: 100%; border-radius: 4px; transition: width 0.3s; min-width: 4px; }\n    .bar-http { background: #3b82f6; }\n    .bar-ws { background: #8b5cf6; }\n    .bar-val { min-width: 60px; font-size: 12px; color: #94a3b8; }\n  </style>\n</head>\n<body>\n  <h1>HTTP vs WebSocket 通信コスト比較</h1>\n\n  <button class=\"btn-http\" id=\"startHttp\">HTTPポーリング開始</button>\n  <button class=\"btn-ws\" id=\"startWs\">WebSocket接続（模擬）</button>\n  <button class=\"btn-stop\" id=\"stopAll\">すべて停止</button>\n\n  <div style=\"display:grid; grid-template-columns:1fr 1fr; gap:12px; margin-top:16px\">\n    <div>\n      <h2 style=\"font-size:16px\">HTTPポーリング</h2>\n      <div class=\"card\" id=\"httpStats\"></div>\n      <div class=\"log\" id=\"httpLog\">待機中...</div>\n    </div>\n    <div>\n      <h2 style=\"font-size:16px\">WebSocket（模擬）</h2>\n      <div class=\"card\" id=\"wsStats\"></div>\n      <div class=\"log\" id=\"wsLog\">待機中...</div>\n    </div>\n  </div>\n\n  <h2>通信コスト比較</h2>\n  <div id=\"chart\"></div>\n\n  <script>\n\n  </script>\n</body>\n</html>",
        "hints": [
          "HTTPポーリングの模擬は setInterval で2000msごとに実行します。実際のfetchではCORSエラーになるため、「HTTPリクエスト送信シミュレーション」として定数サイズ（ヘッダー約800B + ボディ50B）を加算する方式で統計を計算してください。",
          "WebSocketの模擬も setInterval で500msごとにメッセージを受信した体で統計を更新します。WebSocketのメッセージはヘッダーオーバーヘッドが小さい（2〜10B）ため、ボディのみのサイズを加算します。httpStats・wsStatsのHTMLはupdateStats()のような関数にまとめて毎回innerHTML書き換えが管理しやすいです。",
          "バーチャートはHTTPとWSの「合計データ転送量」を取得し、大きい方を100%として幅を計算します。例: const max = Math.max(httpTotal, wsTotal, 1); の後、httpBarの幅を (httpTotal/max*100) + '%' に設定します。stopAll では clearInterval で両タイマーを止めます。"
        ],
        "answer": "<!DOCTYPE html>\n<html lang=\"ja\">\n<head>\n  <meta charset=\"UTF-8\">\n  <title>HTTP vs WebSocket シミュレーター</title>\n  <style>\n    body { font-family: sans-serif; padding: 20px; background: #0f172a; color: #e2e8f0; max-width: 780px; }\n    h2 { color: #3b82f6; border-bottom: 1px solid #334155; padding-bottom: 8px; margin-top: 24px; }\n    .card { background: #1e293b; border-radius: 8px; padding: 16px; margin-bottom: 12px; min-height: 100px; }\n    .stat { display: flex; justify-content: space-between; padding: 6px 0; border-bottom: 1px solid #334155; font-size: 14px; }\n    .stat:last-child { border-bottom: none; }\n    .stat-val { color: #fbbf24; font-weight: bold; }\n    button { padding: 8px 16px; color: white; border: none; border-radius: 6px; cursor: pointer; margin-right: 8px; margin-bottom: 8px; }\n    .btn-http { background: #3b82f6; } .btn-ws { background: #8b5cf6; } .btn-stop { background: #ef4444; }\n    .log { height: 120px; overflow-y: auto; background: #0f172a; border-radius: 6px; padding: 8px; font-family: monospace; font-size: 12px; color: #94a3b8; margin-top: 8px; }\n    .bar-container { display: flex; align-items: center; gap: 12px; margin-bottom: 8px; }\n    .bar-label { min-width: 130px; font-size: 13px; }\n    .bar-track { flex: 1; background: #334155; border-radius: 4px; height: 20px; overflow: hidden; }\n    .bar-fill { height: 100%; border-radius: 4px; transition: width 0.4s; }\n    .bar-http { background: #3b82f6; } .bar-ws { background: #8b5cf6; }\n    .bar-val { min-width: 80px; font-size: 12px; color: #94a3b8; }\n  </style>\n</head>\n<body>\n  <h1>HTTP vs WebSocket 通信コスト比較</h1>\n  <button class=\"btn-http\" id=\"startHttp\">HTTPポーリング開始</button>\n  <button class=\"btn-ws\" id=\"startWs\">WebSocket接続（模擬）</button>\n  <button class=\"btn-stop\" id=\"stopAll\">すべて停止</button>\n  <div style=\"display:grid; grid-template-columns:1fr 1fr; gap:12px; margin-top:16px\">\n    <div>\n      <h2 style=\"font-size:16px;margin-top:0\">HTTPポーリング</h2>\n      <div class=\"card\" id=\"httpStats\"><span style=\"color:#475569\">待機中</span></div>\n      <div class=\"log\" id=\"httpLog\">待機中...</div>\n    </div>\n    <div>\n      <h2 style=\"font-size:16px;margin-top:0\">WebSocket（模擬）</h2>\n      <div class=\"card\" id=\"wsStats\"><span style=\"color:#475569\">待機中</span></div>\n      <div class=\"log\" id=\"wsLog\">待機中...</div>\n    </div>\n  </div>\n  <h2>通信コスト比較（合計データ転送量）</h2>\n  <div id=\"chart\"><span style=\"color:#475569\">開始後に表示されます</span></div>\n  <script>\n    let httpTimer = null, wsTimer = null;\n    const http = { requests: 0, headerBytes: 0, bodyBytes: 0 };\n    const wss = { connects: 0, messages: 0, bytes: 0, handshake: 850 };\n    const HTTP_HEADER = 820, HTTP_BODY = 60, WS_MSG = 55, WS_FRAME = 6;\n    const HTTP_INTERVAL = 2000, WS_INTERVAL = 600;\n    function addLog(id, msg) {\n      const el = document.getElementById(id);\n      const line = document.createElement('div');\n      line.textContent = `[${new Date().toLocaleTimeString('ja')}] ${msg}`;\n      el.appendChild(line);\n      el.scrollTop = el.scrollHeight;\n    }\n    function fmtBytes(b) { return b < 1024 ? b + 'B' : (b/1024).toFixed(1) + 'KB'; }\n    function updateHttpStats() {\n      const total = http.headerBytes + http.bodyBytes;\n      document.getElementById('httpStats').innerHTML = [\n        { l: 'リクエスト数', v: http.requests + '回' },\n        { l: 'ヘッダーオーバーヘッド', v: fmtBytes(http.headerBytes) },\n        { l: 'ボディデータ', v: fmtBytes(http.bodyBytes) },\n        { l: '合計転送量', v: fmtBytes(total) },\n      ].map(s => `<div class=\"stat\"><span>${s.l}</span><span class=\"stat-val\">${s.v}</span></div>`).join('');\n    }\n    function updateWsStats() {\n      const total = wss.handshake + wss.bytes;\n      document.getElementById('wsStats').innerHTML = [\n        { l: '接続確立（ハンドシェイク）', v: '1回' },\n        { l: 'メッセージ受信数', v: wss.messages + '件' },\n        { l: 'フレームオーバーヘッド', v: fmtBytes(wss.messages * WS_FRAME) },\n        { l: '合計転送量', v: fmtBytes(total) },\n      ].map(s => `<div class=\"stat\"><span>${s.l}</span><span class=\"stat-val\">${s.v}</span></div>`).join('');\n    }\n    function updateChart() {\n      const httpTotal = http.headerBytes + http.bodyBytes;\n      const wsTotal = wss.handshake + wss.bytes;\n      const max = Math.max(httpTotal, wsTotal, 1);\n      document.getElementById('chart').innerHTML = [\n        { label: 'HTTP ポーリング', val: httpTotal, cls: 'bar-http' },\n        { label: 'WebSocket（模擬）', val: wsTotal, cls: 'bar-ws' },\n      ].map(b => `\n        <div class=\"bar-container\">\n          <div class=\"bar-label\">${b.label}</div>\n          <div class=\"bar-track\"><div class=\"bar-fill ${b.cls}\" style=\"width:${(b.val/max*100).toFixed(1)}%\"></div></div>\n          <div class=\"bar-val\">${fmtBytes(b.val)}</div>\n        </div>`).join('');\n    }\n    document.getElementById('startHttp').addEventListener('click', () => {\n      if (httpTimer) return;\n      addLog('httpLog', 'HTTPポーリング開始（2秒間隔）');\n      httpTimer = setInterval(() => {\n        http.requests++;\n        http.headerBytes += HTTP_HEADER;\n        http.bodyBytes += HTTP_BODY;\n        const fake = Math.floor(Math.random() * 50 + 10);\n        addLog('httpLog', `GET /api/realtime → 200 OK (ヘッダー:${HTTP_HEADER}B + データ:${HTTP_BODY}B)`);\n        updateHttpStats();\n        updateChart();\n      }, HTTP_INTERVAL);\n    });\n    document.getElementById('startWs').addEventListener('click', () => {\n      if (wsTimer) return;\n      addLog('wsLog', 'WebSocket接続確立（ハンドシェイク850B）');\n      wss.connects++;\n      updateWsStats();\n      wsTimer = setInterval(() => {\n        wss.messages++;\n        wss.bytes += WS_MSG + WS_FRAME;\n        addLog('wsLog', `受信: {activeUsers: ${Math.floor(Math.random()*500+100)}} (${WS_MSG + WS_FRAME}B)`);\n        updateWsStats();\n        updateChart();\n      }, WS_INTERVAL);\n    });\n    document.getElementById('stopAll').addEventListener('click', () => {\n      clearInterval(httpTimer); clearInterval(wsTimer);\n      httpTimer = null; wsTimer = null;\n      addLog('httpLog', '停止しました'); addLog('wsLog', '接続を切断しました');\n    });\n  </script>\n</body>\n</html>"
      }
    },
    {
      "type": "quiz",
      "data": {
        "questions": [
          {
            "q": "WebSocketがHTTPポーリングよりもサーバー・ブラウザ間の通信効率が高い主な理由はどれですか？",
            "options": ["WebSocketはHTTPより高速なプロトコルを使うため", "WebSocketは一度接続を確立すると軽量フレームで双方向通信でき、毎回のHTTPヘッダーオーバーヘッドが不要なため", "WebSocketはデータを圧縮して送るため", "WebSocketはUDPを使うため信頼性が低い代わりに速い"],
            "correct": 1,
            "explanation": "HTTPポーリングでは毎回数百バイトのリクエストヘッダーが発生します。WebSocketは最初のハンドシェイクのみHTTPを使い、以降は2〜10バイト程度の軽量フレームで双方向通信します。長期間・高頻度のリアルタイム通信では転送量の差が大きくなります。"
          },
          {
            "q": "WebSocketの接続確立時に使用されるHTTPステータスコードはどれですか？",
            "options": ["200 OK", "301 Moved Permanently", "101 Switching Protocols", "204 No Content"],
            "correct": 2,
            "explanation": "WebSocketの接続確立は通常のHTTPリクエストに `Upgrade: websocket` ヘッダーを付けて開始します。サーバーが承認すると `101 Switching Protocols` を返し、以降の通信がWebSocketプロトコルに切り替わります。"
          },
          {
            "q": "GA4のリアルタイムレポートやチャットボットで使われる「サーバーからブラウザへの一方向プッシュ」に適したHTTPとWebSocketの中間的な技術はどれですか？",
            "options": ["XMLHttpRequest（XHR）", "WebRTC", "Server-Sent Events（SSE）", "JSONP"],
            "correct": 2,
            "explanation": "Server-Sent Events（SSE）はHTTPのままサーバーからブラウザへの一方向プッシュを実現する技術です。WebSocketのように双方向ではありませんが、既存のHTTPインフラをそのまま使えます。GA4リアルタイムレポートやAIチャットのストリーミング表示に使われることがあります。"
          }
        ]
      }
    },
    {
      "type": "summary",
      "data": {
        "content": "# モジュール0-9 完了！\n\n## 習得したスキル\n\n- HTTPポーリングとWebSocketの通信モデルの違いを説明できる\n- WebSocketがHTTPアップグレード（101 Switching Protocols）で確立されることを理解した\n- WebSocket・SSE・HTTPポーリングの使いどころを使い分けられる\n- GA4リアルタイムレポートやチャットボットがなぜ即時更新できるかを説明できる\n- 通信コストの観点でHTTPポーリングとWebSocketを比較できる\n\n## 次のモジュール\n\n**0-10: APIエンドポイントとHTTPメソッドの対応** では、このシリーズの総仕上げとしてREST APIの設計パターンを学びます。GA4 Measurement Protocol・Meta Conversions API・Google広告APIのエンドポイントを読み解き、マーケターが直接APIを扱えるようになることを目標にします。\n\n## 実務チャレンジ\n\n担当サイトにチャットボットや問い合わせフォームがあれば、Chrome DevToolsのNetworkタブを開いてWS（WebSocket）フィルタを適用してみてください。WebSocket接続が確立されているかどうかを確認し、メッセージの内容をFramesタブで覗いてみましょう。",
        "diagram": false
      }
    }
  ]
}
